import TopCoins from "./TopCoins";
import TotalBalance from "./TotalBalance";

export default function Dashboard() {
  return (
    <section>
      <TotalBalance />
      <TopCoins />
    </section>
  );
}
